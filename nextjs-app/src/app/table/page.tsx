import React from 'react';
import data from '../data/data';

const TablePage = () => {
	const dataForTable = data;
	return (
		<main className="flex min-h-screen flex-col items-center justify-between p-8"></main>
	);
};

export default TablePage;
